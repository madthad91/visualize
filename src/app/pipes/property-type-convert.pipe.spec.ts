/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PropertyTypeConvertPipe } from './property-type-convert.pipe';

describe('Pipe: PropertyTypeConvert', () => {
  it('create an instance', () => {
    let pipe = new PropertyTypeConvertPipe();
    expect(pipe).toBeTruthy();
  });
});
